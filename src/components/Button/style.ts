import styled from 'styled-components'

export const StyledButton = styled.button`
  background: royalblue;
  color: white;
  font-weight: 600;
  border: none;
  font-size: 14px;
  cursor: pointer;

  &:focus {
    outline: none;
  }
`
